# From host machine when Postgres is exposed on localhost:5433
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5433/retail_bi

# From Docker Compose service network, use:
# DATABASE_URL=postgresql+psycopg://postgres:postgres@postgres:5432/retail_bi
